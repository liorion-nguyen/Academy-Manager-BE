export enum Gender {
    Male = 'male',
    Female = 'female',
    Other = 'other',
}

export enum Role {
    Student = "student",
    Teacher = "teacher",
    Admin = "admin"
}

export enum Sort {
    desc = "desc",
    asc = "asc",
    DESC = "DESC",
    ASC = "ASC",
}